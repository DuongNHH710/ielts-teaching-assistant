<UserControl
    x:Class="IeltsTeachingAssistant.Controls.BandScoreSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IeltsTeachingAssistant.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="8">
        <TextBlock x:Name="HeaderTextBlock" FontWeight="SemiBold" />
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Slider x:Name="ScoreSlider" Minimum="0" Maximum="9" StepFrequency="0.5" TickFrequency="0.5" TickPlacement="Outside" Value="{x:Bind Value, Mode=TwoWay}" />
            <Border Grid.Column="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="{StaticResource SmallCornerRadius}" Padding="12,4" MinWidth="60">
                <TextBlock Text="{x:Bind Value, Mode=OneWay, FallbackValue='0.0'}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="16" />
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
