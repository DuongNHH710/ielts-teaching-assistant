<ContentDialog
    x:Class="IeltsTeachingAssistant.Views.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Login Required"
    PrimaryButtonText="Login"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel Spacing="16" Width="300" Margin="0,16,0,0">
        <TextBlock Text="Please enter your admin password to access the IELTS Teaching Assistant." TextWrapping="Wrap" />

        <StackPanel Spacing="8">
            <TextBlock Text="Password" FontWeight="SemiBold" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" KeyDown="PasswordBox_KeyDown" />
        </StackPanel>

        <TextBlock x:Name="ErrorText" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="Collapsed" Text="Incorrect password. Please try again." />
    </StackPanel>
</ContentDialog>
