<Page
    x:Class="IeltsTeachingAssistant.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border Style="{StaticResource PremiumCardStyle}" 
                HorizontalAlignment="Center" VerticalAlignment="Center" 
                Width="400" Padding="40">
            <StackPanel Spacing="24">
                <FontIcon Glyph="&#xE82D;" FontSize="48" Foreground="{ThemeResource AccentFillColorDefaultBrush}" HorizontalAlignment="Center" />
                
                <StackPanel Spacing="8">
                    <TextBlock Text="Welcome Back" Style="{StaticResource SectionHeaderStyle}" HorizontalAlignment="Center" />
                    <TextBlock Text="Enter your master password to continue" Opacity="0.6" HorizontalAlignment="Center" />
                </StackPanel>

                <PasswordBox Header="Password" Password="{x:Bind ViewModel.Password, Mode=TwoWay}" />
                
                <Button Content="Login" Command="{x:Bind ViewModel.LoginCommand}" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
